% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SHASH.R
\name{SHASH_transform}
\alias{SHASH_transform}
\alias{SHASH_to_normal}
\alias{normal_to_SHASH}
\title{SHASH Data Transformation}
\usage{
SHASH_to_normal(x, mu, sigma, nu, tau)

normal_to_SHASH(x, mu, sigma, nu, tau)
}
\arguments{
\item{x}{Numeric vector of values to transform.}

\item{mu}{Numeric scalar. Location parameter controlling the mean of the
SHASH distribution.}

\item{sigma}{Numeric scalar. Spread parameter on the log scale. The function
applies \code{exp(sigma)} internally, so pass the raw coefficient as
returned by \code{gamlssML()}. Pass \code{sigma = 0} to get unit spread
since \code{exp(0) = 1}.}

\item{nu}{Numeric scalar. Skewness parameter. A value of 0 gives a
symmetric distribution.}

\item{tau}{Numeric scalar. Tail-weight parameter on the log scale. Pass
\code{tau = 0} for normal-like tails since \code{exp(0) = 1}.}
}
\value{
A numeric vector of transformed values, the same length as \code{x}.
}
\description{
These two functions form a matched pair for transforming data between the
SHASH (Sinh-Arcsinh) distribution and the standard normal distribution.
\code{SHASH_to_normal()} maps SHASH-distributed observations onto an
approximately normal scale; \code{normal_to_SHASH()} is the inverse.
}
\section{Functions}{
\itemize{
\item \code{SHASH_to_normal()}: Transforms SHASH-distributed data to
approximately normal data.

\item \code{normal_to_SHASH()}: Transforms standard normal data back to the
SHASH-distributed scale.

}}
\examples{
set.seed(42)
x <- rnorm(200)
x[c(17, 77)] <- x[c(17, 77)] + 5

mu <- 0; sigma <- 0; nu <- 0; tau <- 0

z <- SHASH_to_normal(x, mu = mu, sigma = sigma, nu = nu, tau = tau)
x_recovered <- normal_to_SHASH(z, mu = mu, sigma = sigma, nu = nu, tau = tau)
all.equal(x, x_recovered)

}
